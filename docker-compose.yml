version: '3.8'
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: duckuser
      POSTGRES_PASSWORD: duckpass
      POSTGRES_DB: duckdb
    ports:
      - "5432:5432"
    volumes:
      - ./data/seed.sql:/docker-entrypoint-initdb.d/init.sql